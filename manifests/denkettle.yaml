---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  labels:
    app: denkettle
  name: denkettle
spec:
  # never more than 1 since denkettle is not horizontally scalable
  replicas: 1
  serviceName: denkettle
  selector:
    matchLabels:
      app: denkettle
  template:
    metadata:
      labels:
        app: denkettle
    spec:
      containers:
        - name: main
          image: marctv/minecraft-papermc-server
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: k8s-configmap
            - secretRef:
                name: k8s-secret
          volumeMounts:
            - mountPath: /data
              name: data
          ports:
            - containerPort: 25565
              protocol: TCP
              name: minecraft
            - containerPort: 25575
              protocol: TCP
              name: rcon
          readinessProbe: &probe
            tcpSocket:
              port: 25565
            initialDelaySeconds: 60
            periodSeconds: 10
          livenessProbe: *probe
          tty: true
          stdin: true
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 6Gi